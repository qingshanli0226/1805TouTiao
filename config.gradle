ext{
    isImageLibrary=true //确定image模块是APP 还是库
    android = [
            //配置一些变量，其他模块使用这些变量，来保证各个开发模块版本的一致性
            compileSdkVersion : 28,
            minSdkVersion : 19,
            targetSdkVersion : 28,
            surportVersion : "28.0.0"
    ]
}